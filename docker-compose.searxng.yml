# Evident Video Fact Checker — SearXNG + Redis
# Runs on port 8888 to avoid conflicts with other SearXNG instances
# Start: docker compose -f docker-compose.searxng.yml up -d
# Stop:  docker compose -f docker-compose.searxng.yml down

services:
  redis:
    image: redis:7-alpine
    container_name: evident-redis
    restart: unless-stopped
    volumes:
      - evident-redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s
    networks:
      - evident-net

  searxng:
    image: searxng/searxng:latest
    container_name: evident-searxng
    restart: unless-stopped
    ports:
      - "8888:8080"
    volumes:
      - ./searxng:/etc/searxng:rw
    environment:
      - SEARXNG_BASE_URL=http://localhost:8888
      - BIND_ADDRESS=0.0.0.0:8080
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/healthz"]
      interval: 20s
      timeout: 5s
      retries: 5
      start_period: 30s
    networks:
      - evident-net

volumes:
  evident-redis-data:

networks:
  evident-net:
    driver: bridge
